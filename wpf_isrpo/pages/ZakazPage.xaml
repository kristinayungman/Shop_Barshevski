<Page x:Class="UserApp.pages.ZakazPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Мои заказы">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" x:Name="OrderGrid" AutoGenerateColumns="False" IsReadOnly="True" Margin="8" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер" Binding="{Binding id}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Товар" Binding="{Binding Product.name}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding timestamp_text}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding status_text}" Width="*" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
        <DockPanel Grid.Row="1"  LastChildFill="False" Height="44">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="Товары" Click="NavProducts_Click" Margin="4"/>
                <Button Content="Сделать заказ" Click="NavCheckout_Click" Margin="4"/>
                <Button Content="Мои заказы" Click="NavOrders_Click" Margin="4"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Назад" Click="NavBack_Click" Margin="4"/>
                <Button Content="Выйти" Click="NavExit_Click" Margin="4"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Page>
