<Page x:Class="UserApp.pages.AutorisationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Авторизация">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="3*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Grid.Row="1" Grid.Column="1" BorderThickness="3"  CornerRadius="24" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock TextAlignment="Center" VerticalAlignment="Center" FontSize="50">Авторизация</TextBlock>
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock FontSize="20">Введите Login</TextBlock>
                    <TextBox Text="{Binding email}" BorderThickness="2" Width="350" Height="30" Padding="10 2" FontSize="20"/>
                </StackPanel>
                <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock FontSize="20">Введите Пароль</TextBlock>
                    <TextBox Text="{Binding password}" BorderThickness="2" Width="350" Height="30" Padding="10 2" FontSize="20"/>
                </StackPanel>
                <StackPanel Grid.Row="3" HorizontalAlignment="Center" Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
            Width="150" Height="50"
            Content="Войти" FontSize="25"
            Click="Auto_Click"
            Margin="0 0 15 0"/>

                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
            Width="90" Height="50" 
            Content="Рег-я" FontSize="25" 
            Click="Register_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
