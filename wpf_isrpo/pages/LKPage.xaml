<Page x:Class="UserApp.pages.LKPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      DataContext="{Binding RelativeSource={RelativeSource Self}}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Каталог">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="WelcomeBlock" Grid.Column="0" VerticalAlignment="Center" FontSize="18"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Label Content="Поиск" Margin="0,0,5,0"/>
                    <TextBox x:Name="SearchTextBox" Width="220" TextChanged="TextBox_TextChanged"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ListView x:Name="ProductList" Margin="8,0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="2" CornerRadius="10" Padding="10" Background="White">
                                <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding name}" FontSize="20" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Category.name}" FontSize="20"/>
                                    <TextBlock Text="{Binding description}" FontSize="20"/>
                                    <TextBlock Text="{Binding price}" FontSize="20"  FontWeight="Bold" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!--<StackPanel Grid.Column="1" Margin="8" MinWidth="160">
                    <Label Content="Фильтр по категории"/>
                    <ComboBox x:Name="CategoriesCombo" SelectionChanged="FilterComboBox_SelectionChanged"/>
                    <Button Click="ResetBtn" Margin="0,16,0,0" HorizontalAlignment="Stretch">Сбросить фильтры</Button>
                </StackPanel>-->
            </Grid>
        </Grid>

        <DockPanel Grid.Row="1"  LastChildFill="False" Height="44">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="Товары" Click="NavProducts_Click" Margin="4,2"/>
                <Button Content="Сделать заказ" Click="NavCheckout_Click" Margin="4,2"/>
                <Button Content="Мои заказы" Click="NavOrders_Click" Margin="4,2"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Label Content="Фильтр по категории"/>
                <ComboBox x:Name="CategoriesCombo" SelectionChanged="FilterComboBox_SelectionChanged"/>
                <Button Click="ResetBtn" Margin="4,2">Сбросить фильтры</Button>
                <Button Content="Назад" Click="NavBack_Click" Margin="4,2"/>
                <Button Content="Выйти" Click="NavExit_Click" Margin="4,2"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Page>
