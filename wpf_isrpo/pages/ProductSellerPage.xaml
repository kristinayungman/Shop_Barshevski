<Page x:Class="UserApp.pages.ProductSellerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Товары">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" x:Name="ProductGrid" Margin="8,8,8,4" AutoGenerateColumns="False" CanUserAddRows="False" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding name}" Width="*"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding description}" Width="*"/>
                <DataGridComboBoxColumn Header="Категория" x:Name="categoryCombo" SelectedItemBinding="{Binding Category}" DisplayMemberPath="name" Width="*"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding count}" Width="90"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding price}" Width="90"/>
                <DataGridTextColumn Header="Фото" Binding="{Binding photo}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="8,0,8,4">
            <Button x:Name="RemoveBtn" Click="RemoveBtn_Click" Margin="0,0,8,0">Удалить</Button>
            <Button x:Name="btnAddProduct" Content="Добавить товар" Click="btnAddProduct_Click"/>
        </StackPanel>
        <DockPanel Grid.Row="2"  LastChildFill="False" Height="44">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="Товары" Click="NavProducts_Click" Margin="4,2"/>
                <Button Content="Заказы" Click="NavOrders_Click" Margin="4,2"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Назад" Click="NavBack_Click" Margin="4,2"/>
                <Button Content="Выйти" Click="NavExit_Click" Margin="4,2"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Page>
