<Page x:Class="UserApp.pages.AddProductPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Новый товар">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20,16,20,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Название:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1" Margin="5" Height="28" Text="{Binding name}"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Описание:" VerticalAlignment="Top" Margin="0,6,0,0"/>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="5" MinHeight="60" TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding description}"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Категория:" VerticalAlignment="Center"/>
            <ComboBox x:Name="categoriesCombo" Grid.Row="2" Grid.Column="1" Margin="5"
                      DisplayMemberPath="name"
                      SelectedItem="{Binding Category}"/>

            <Label Grid.Row="3" Grid.Column="0" Content="Цена:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="3" Grid.Column="1" Margin="5" Height="28" Text="{Binding price}"/>

            <Label Grid.Row="4" Grid.Column="0" Content="Количество:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="4" Grid.Column="1" Margin="5" Height="28" Text="{Binding count}"/>

            <Label Grid.Row="5" Grid.Column="0" Content="Фото:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="5" Grid.Column="1" Margin="5" Height="28" Text="{Binding photo}"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
            <Button x:Name="btnSave" Content="Сохранить" MinWidth="120" Margin="6" Click="btnSave_Click"/>
            <Button x:Name="btnCancel" Content="Отмена" MinWidth="120" Margin="6" Click="btnCancel_Click"/>
        </StackPanel>

        <DockPanel Grid.Row="2"  LastChildFill="False" Height="44">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="Товары" Click="NavProducts_Click" Margin="4,2"/>
                <Button Content="Заказы" Click="NavOrders_Click" Margin="4,2"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Назад" Click="NavBack_Click" Margin="4,2"/>
                <Button Content="Выйти" Click="NavExit_Click" Margin="4,2"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Page>
