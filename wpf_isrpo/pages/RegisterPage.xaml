<Page x:Class="UserApp.pages.RegisterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Регистрация">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="5*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Grid.Row="1" Grid.Column="1" BorderThickness="3"  CornerRadius="24" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock TextAlignment="Center" VerticalAlignment="Center" FontSize="50">Регистрация</TextBlock>
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock FontSize="20">Введите ФИО</TextBlock>
                    <TextBox Text="{Binding fio}" BorderThickness="2" Width="350" Height="30" Padding="10 2" FontSize="20"/>
                </StackPanel>
                <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock FontSize="20">Введите Login</TextBlock>
                    <TextBox Text="{Binding email}" BorderThickness="2" Width="350" Height="30" Padding="10 2" FontSize="20"/>
                </StackPanel>
                <StackPanel Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock FontSize="20">Введите Пароль</TextBlock>
                    <TextBox Text="{Binding password}" BorderThickness="2" Width="350" Height="30" Padding="10 2" FontSize="20"/>
                </StackPanel>
                <StackPanel Grid.Row="4"    HorizontalAlignment="Center" 
            VerticalAlignment="Center"
            Orientation="Horizontal">

                    <Border CornerRadius="25" >
                        <Button Content="Зарегистрироваться" 
                FontSize="18" Width="280" Height="50"
                Click="Register_Click" Cursor="Hand"/>
                    </Border>

                    <Border CornerRadius="25"  Margin="10 0 0 0">
                        <Button Content="Авт-ия" 
                FontSize="18" Width="80" Height="50"
                Click="Auto_Click"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
