<Page x:Class="UserApp.pages.CheckoutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Оформление заказа">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="1"  LastChildFill="False" Height="44">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="Товары" Click="NavProducts_Click" Margin="4,2"/>
                <Button Content="Сделать заказ" Click="NavCheckout_Click" Margin="4,2"/>
                <Button Content="Мои заказы" Click="NavOrders_Click" Margin="4,2"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Назад" Click="NavBack_Click" Margin="4,2"/>
                <Button Content="Выйти" Click="NavExit_Click" Margin="4,2"/>
            </StackPanel>
        </DockPanel>
        <Grid Grid.Row="0" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Отметьте товары и нажмите «Оформить заказ»" FontSize="20" Margin="0,0,0,8"/>
            <ListView x:Name="PickList" Grid.Row="1" Background="White">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Content="{Binding Caption}" Margin="4" VerticalContentAlignment="Center"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Grid.Row="2" Content="Оформить заказ" HorizontalAlignment="Center" Margin="0,12,0,0" Padding="16,6" Click="Submit_Click"/>
        </Grid>
    </Grid>
</Page>
